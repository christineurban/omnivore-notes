# Misc

**Naming Conventions**
 `future_input_schema` = requests
 `future_schema` = response
 write to Exchange
 read from Queue
 

## Environment
**Import error**
 - activate
 `. activate`
- restart
`deactivate`
- rebuild
`rm -rf env`
`./init.sh`